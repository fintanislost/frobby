<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Match Protocol's floor so we test the exact types consumers see. -->
    <TargetFramework>net6.0</TargetFramework>
    <IsPackable>false</IsPackable>
    <RootNamespace>SdvTestFramework.Protocol.Tests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.10.0" />
    <PackageReference Include="xunit" Version="2.9.0" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
    <!-- Used by DuplexStreamFixture (in-memory full-duplex stream pair for session tests). -->
    <PackageReference Include="System.IO.Pipelines" Version="8.0.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Protocol\Protocol.csproj" />
  </ItemGroup>
</Project>
