{
  "name": "draw_contains_sample_marker",
  "fixture": "m0spike_436515781",
  "config": { "seed": 42 },
  "steps": [
    { "action": "draw.arm", "args": { "ticks": 60 } },
    { "action": "wait.ms", "args": { "ms": 2000 } }
  ],
  "assertions": [
    {
      "type": "draw.contains",
      "filter": { "texture_asset": "LooseSprites/Cursors" },
      "min_count": 1,
      "message": "the cursor atlas (patched by the sample mod) should render at least once"
    }
  ]
}
